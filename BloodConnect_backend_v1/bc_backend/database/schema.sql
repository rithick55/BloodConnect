CREATE DATABASE IF NOT EXISTS bloodconnect CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE bloodconnect;
-- Spring Boot/JPA creates and updates the tables from the entities.
-- Run this file once, then set your MySQL password in application.properties.
